import { LucideIcon } from 'lucide-react';

export interface Activity {
  id: string;
  name: string;
  icon: LucideIcon;
  color: string;
  textColor: string;
  borderColor: string;
  shadow: string;
  iconName?: string;
}



